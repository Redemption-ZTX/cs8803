# v074d_failure_bucket_orthogonal

Failure-mode-orthogonal deploy-time ensemble:

- `055@1150` — late_defensive_collapse + low_possession anchor.
- `055v2@1000` — elevated `poor_conversion` (distinctive vs 055).
- `053Acont@430` — higher `unclear_loss` share + PBRS blood.

See `docs/experiments/snapshot-074D-failure-bucket-orthogonal.md` for the
full bucket table from `docs/experiments/artifacts/failure-cases/`.

Caveats:
- Extracted bucket counts come from filename suffixes; they are the same
  labels used in `failure-capture-logs/*.log`. Exact relative share differs
  because total save counts differ per run.
- `055v2` path needs verification before launch — the ray_results dir
  name is derived from the launch script, not directly verified here.
