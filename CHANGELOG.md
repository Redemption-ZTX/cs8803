# Changelog

格式基于 [Keep a Changelog](https://keepachangelog.com/)。

<!-- 模板：

## [vX.Y.Z] - YYYY-MM-DD

### Added
- `docs/experiments/snapshot-025-bc-champion-field-role-binding.md` to preregister a conservative field-role-binding fine-tune on top of the current `BC -> MAPPO` champion (`017@2100`), explicitly reframing the question from “can 024 work?” to “can field-role binding still improve an already champion-grade policy?”
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_field_role_binding_bc2100_512x512.batch` as the first runnable `025` batch: it warm-starts from `017@2100`, keeps `spawn_depth` role binding, weakens the 024 role asymmetry coefficients, and uses a shorter `8M`-step conservative fine-tune budget.
### Changed
- `docs/experiments/snapshot-020-mappo-v4-fair-ablation.md` now records the completed first-round `baseline 500` + failure-capture readout for MAPPO+v4: the best official point is only `checkpoint-490 = 0.764`, and the remaining failure mix is still dominated by `late_defensive_collapse + low_possession`, so the evidence does not support promoting `v4 shaping` into the current MAPPO mainline.
- `docs/experiments/snapshot-019-mappo-v2-opponent-pool-anchor-heavy-rebalance.md` now records the completed first-round official recheck + failure-capture verdict: the anchor-heavy pool lane does produce a real early window (`checkpoint-40/140 = 0.788`), but the advantage fades with longer training, so this branch is best understood as an early-window lane rather than a new long-train mainline.
- `docs/experiments/README.md` now indexes SNAPSHOT-019 and SNAPSHOT-020 as completed first-round result snapshots instead of still-pending evaluation runs.
### Fixed
### Removed

-->

## [Unreleased]

### Added
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_liberation_D_warm_bc2100_512x512.batch` as the runnable orthogonal control arm for `SNAPSHOT-026`: keep shaping-v2 exactly intact on top of `BC@2100`, but add `ENTROPY_COEFF=0.01` to test whether the current ceiling is driven more by policy collapse than by reward design.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_liberation_C_warm_bc2100_512x512.batch` as the runnable `SNAPSHOT-026` Path-C warm-start arm: it keeps the same BC@2100 base and conservative PPO settings as A/B, but removes all per-step shaping and rewards only sparse goals plus event-based shot/tackle/clearance signals.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_liberation_A_warm_bc2100_512x512.batch` and `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_liberation_B_warm_bc2100_512x512.batch` as the first runnable warm-start arms for `SNAPSHOT-026`: Path A removes penalty-style shaping but keeps positive ball-progress + possession, while Path B replaces dx-style progress with goal-proximity PBRS and keeps possession.
- `docs/experiments/snapshot-025b-bc-champion-field-role-binding-stability-tune.md` to preregister a clean parallel control arm for the ongoing `025` run: it keeps the `BC@2100 + spawn_depth field-role binding` mechanism unchanged and only tightens PPO optimization (`lr`, `num_sgd_iter`, `sgd_minibatch_size`, `clip_param`) so we can tell whether the current repeated `kl/total_loss=inf` spikes are mainly an optimization-stability issue.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_field_role_binding_bc2100_stable_512x512.batch` as the runnable `025b` batch: same warm-start and same role coefficients as `025`, but with `LR=1e-4`, `NUM_SGD_ITER=4`, `SGD_MINIBATCH_SIZE=2048`, and `CLIP_PARAM=0.15` for a cleaner side-by-side comparison.
- `docs/management/cleanup-log.md` to keep an append-only record of disk cleanup operations, including deleted directories, rationale, and quota before/after snapshots.

### Changed
- `docs/experiments/snapshot-013-baseline-weakness-analysis.md`, `docs/experiments/snapshot-015-behavior-cloning-team-bootstrap.md`, and `docs/experiments/README.md` now close an older documentation-sync gap around the IL/BC support line: `SNAPSHOT-013` is explicitly marked as a completed first-round baseline-weakness study rather than still “预注册 / 进行中”, `SNAPSHOT-012` is indexed as a completed teacher-data phase that hands off to `015`, and `SNAPSHOT-015` now records the formal team-level BC training asset [BC_team_baseline_selfplay_512x512_20260413_033033](ray_results/BC_team_baseline_selfplay_512x512_20260413_033033) with `best_epoch=30` and `checkpoint_000030` instead of stopping at smoke-only validation.
- `docs/experiments/snapshot-026-reward-liberation-ablation.md` and `docs/experiments/README.md` now record the completed first-round `A/B/C/D-warm` readout instead of only the preregistration: `A-warm` currently has the highest ceiling (`0.96 @ 290`), `B-warm` forms the broadest PBRS platform (`0.86~0.90` over a wide mid/late window), `C-warm` remains positive but unstable, and `D-warm` reaches the same `0.90` class with `0` bad iterations, which sharply raises the possibility that exploration/entropy—not only reward liberation—is a core part of the current performance bottleneck.
- `docs/experiments/snapshot-027-team-level-ppo-coordination.md`, `docs/experiments/README.md`, and `docs/management/WORKLOG.md` now record the completed first-round verdict for `027A`: after fixing the `checkpoint-700`-induced eval gap via backfill and re-running official selection with the same `top 5% + ties + ±2 window` protocol used for `028A`, the best official `baseline 500` closes at `checkpoint-650/1230 = 0.804`. This upgrades `027A` from “resumed run in progress” to a completed team-level scratch positive result with a real but limited ceiling around `0.80`, strengthening the reading that `028A`’s BC warm-start adds genuine value beyond the team-level architecture alone.
- `docs/experiments/snapshot-028-team-level-bc-to-ppo-bootstrap.md`, `docs/experiments/README.md`, and `docs/management/WORKLOG.md` now record the first-round `028A` official + failure-capture verdict: although the late official peak reaches `checkpoint-1220 = 0.844`, the more trustworthy main candidate contracts to `checkpoint-1060 = 0.810 official / 0.806 capture` after `500ep` failure review. This keeps `028A` as a real improvement over `027A`’s team-level scratch ceiling, while still leaving the question of whether team-level BC warm-start can truly challenge `025b / 029B` to the follow-up head-to-heads.
- `docs/experiments/snapshot-028-team-level-bc-to-ppo-bootstrap.md`, `docs/experiments/README.md`, and `docs/management/WORKLOG.md` now also record the completed `028A` head-to-head verdict: the trustworthy main candidate `checkpoint-1060` clearly beats `027A@650` (`0.592`), confirming that team-level BC warm-start adds real value on top of team-level scratch, but both `1060` and the official peak `1220` still lose clearly to `017`, `025b`, and `029B`. This closes `028A` as a successful team-level mainline validation rather than a champion takeover.
- `docs/experiments/snapshot-025b-bc-champion-field-role-binding-stability-tune.md` and `docs/experiments/README.md` now include the completed `025b` head-to-head verdict on top of the official/failure-capture readout: `025b@70` beats both `017@2100` (`0.556`) and `024@270` (`0.586`), while `025b@80` also beats `017@2100` (`0.562`), so `025b` is no longer just a stable baseline-strong line but the current leading champion candidate.
- `docs/experiments/snapshot-025b-bc-champion-field-role-binding-stability-tune.md`, `docs/experiments/README.md`, and `docs/README.md` now record the completed first-round `025b` verdict instead of only the preregistration: optimizer tightening reduced bad iterations from `84/200` to `5/200`, official `baseline 500` peaked at `checkpoint-80 = 0.842`, and failure capture further identified `checkpoint-70 = 0.836 official / 0.840 capture` as the more balanced main candidate, supporting the conclusion that `025` was limited primarily by optimization instability rather than field-role-binding incompatibility.
- `docs/experiments/snapshot-026-reward-liberation-ablation.md` and `docs/experiments/README.md` now reflect the current four-arm warm-start status more accurately: `A/B/C/D-warm` are all runnable, and `D` is explicitly documented as the entropy-only orthogonal control arm rather than a reward-side liberation path.
- `cs8803drl/core/soccer_info.py`, `cs8803drl/core/utils.py`, `cs8803drl/training/train_ray_team_vs_random_shaping.py`, and `cs8803drl/training/train_ray_mappo_vs_baseline.py` now also support minimal event-based shaping (`SHAPING_EVENT_*`, `SHAPING_SHOT_*`, `SHAPING_CLEARANCE_*`) so `SNAPSHOT-026` can launch Path C without changing the observation/model stack; training headers now print the active event reward and threshold config for smoke verification.
- `cs8803drl/core/soccer_info.py`, `cs8803drl/core/utils.py`, `cs8803drl/training/train_ray_team_vs_random_shaping.py`, and `cs8803drl/training/train_ray_mappo_vs_baseline.py` now support an optional goal-proximity PBRS shaping term (`SHAPING_GOAL_PROXIMITY_*`) without disturbing existing lanes; `SNAPSHOT-026` can therefore launch Path B as a minimal reward-side ablation instead of requiring a broader refactor first.
- `docs/experiments/snapshot-026-reward-liberation-ablation.md` and `docs/experiments/README.md` now mark `A-warm`, `B-warm`, and `C-warm` as runnable while keeping the full 026 matrix preregistered.
- `docs/experiments/README.md`, `docs/README.md`, and `docs/plan/plan-002-il-mappo-dual-mainline.md` now index `SNAPSHOT-025b` as the parallel stability-tuned companion to `025`, explicitly framing it as an optimizer-only repair arm rather than a new mechanism line.
- `docs/experiments/snapshot-025-bc-champion-field-role-binding.md` now records a completed `1-iter` preflight smoke on the BC champion base: the `checkpoint-2100` warm-start copied cleanly (`copied=16`), the conservative defender deep-zone penalties stayed at `0.0045`, and `checkpoint-1` still achieved `0.840 (42W-8L)` vs baseline over 50 episodes, so the long run can proceed.
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md` now records the normalized direct-concat rerun verdict more precisely: the original unnormalized `021b` remained a true negative, but the normalized `021b-norm` lane stabilizes around `0.780 (390W-110L @ 500ep)` under `local_true_teammate_info` at `checkpoint-450`, and its repaired local failure capture now exposes meaningful `late_defensive_collapse / low_possession / poor_conversion` buckets instead of the earlier possession-blind trace output.
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md` now records the completed first-round official `baseline 500` recheck and failure-capture readout for `021c-B`: the auxiliary teammate-prediction route is clearly positive, but the best official point is only `checkpoint-480 = 0.794`, so the lane currently reads as a credible mid/high platform rather than a new champion line.
- `docs/experiments/README.md` now indexes SNAPSHOT-021 with the stricter split: unnormalized `021b` is negative, normalized `021b-norm` is a strong local-true-info positive result (`checkpoint-450 = 0.860 @ 200ep`), and `021c-B` remains the official-aligned positive lane with `checkpoint-480 = 0.794`.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_vs_baseline_shaping_v4_512x512.batch` as the concrete fair-MAPPO launch script for SNAPSHOT-020, keeping the centralized-critic setup fixed while swapping the reward objective from shaping-v2 to shaping-v4.
- `docs/experiments/snapshot-020-mappo-v4-fair-ablation.md` to preregister the MAPPO-side test of the v4 anti-rush / survival objective: before building any team-level PPO-v4 opponent adapter, first verify whether the v4 reward semantics actually improve or even survive inside the fair MAPPO setup.
- `docs/experiments/snapshot-019-mappo-v2-opponent-pool-anchor-heavy-rebalance.md` to preregister the first post-018 opponent-pool weight ablation: keep the fair `MAPPO+v2` warm-start and shaping-v2 objective fixed, but rebalance the pool to `baseline 30% / anchor 30% / v1 20% / no-shaping 20%` in order to test how much baseline anchoring is actually necessary.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_v2_opponent_pool_anchor30_512x512.batch` as the concrete launch script for that aggressive opponent-pool rebalance lane.
- `cs8803drl/training/train_ray_mappo_vs_opponent_pool.py` plus `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_v2_opponent_pool_512x512.batch` as the first static frozen-opponent-pool fine-tune lane on top of the fair MAPPO trainer; the initial pool is `baseline 60% + MAPPO-v2 15% + MAPPO-v1 15% + MAPPO no-shaping 10%`.
- `docs/experiments/snapshot-017-bc-to-mappo-bootstrap.md` to preregister the `BC -> MAPPO` bootstrap line: use the completed baseline-teacher BC asset as initialization for the current strongest `MAPPO + shaping-v2` mainline, while explicitly calling out the current team-level-BC vs player-level-MAPPO actor compatibility gap.
- `docs/experiments/snapshot-016-shaping-v4-survival-anti-rush-ablation.md` to preregister the shaping-v4 line: return to the v2 base, keep the proven defensive shaping, and add a defensive-survival bonus plus a fast-loss terminal penalty aimed at avoiding short baseline blowouts.
- `cs8803drl/branches/imitation_bc.py`, `cs8803drl/training/train_bc_team_policy.py`, `cs8803drl/deployment/trained_bc_team_agent.py`, and `scripts/batch/experiments/soccerstwos_h100_cpu32_bc_team_baseline_selfplay_512x512.batch` as the first minimal team-level behavior-cloning training/deployment loop on top of the collected baseline self-play teacher dataset.
- `snapshot-015-behavior-cloning-team-bootstrap.md` to record the minimal BC trainer, checkpoint format, deployment wrapper, and first smoke validation.
- `cs8803drl/training/train_ray_mappo_vs_baseline.py` as the first fair MAPPO / centralized-critic training entry aligned with the current baseline-targeted eval pipeline.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_vs_baseline_noshaping_512x512.batch`, `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_vs_baseline_shaping_v1_512x512.batch`, and `scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_vs_baseline_shaping_v2_512x512.batch` as the first three L1a MAPPO benchmark runs.
- `docs/plan/plan-002-il-mappo-dual-mainline.md` to formalize the upgraded phase plan: IL/BC and MAPPO as dual mainlines, with baseline weakness analysis as support and ensemble as a late-stage multiplier.
- `snapshot-014-mappo-fair-ablation.md` to preregister a fair centralized-critic / MAPPO benchmark after re-evaluating the old `shared_cc` evidence.
- `docs/plan/README.md` and `docs/plan/plan-001-il-baseline-exploitation.md` to record the new phase-wide pivot from shaping-only PPO toward imitation learning, baseline weakness analysis, and later ensemble work.
- `snapshot-012-imitation-learning-bc-bootstrap.md` to preregister the baseline-teacher trajectory collection and BC bootstrap line.
- `snapshot-013-baseline-weakness-analysis.md` to preregister the opponent-modeling / baseline weakness analysis line.
- `scripts/tools/collect_baseline_trajectories.py` to collect sharded teacher datasets from baseline self-play or other module-vs-module matchups for future imitation learning.
- `scripts/eval/analyze_episode_records.py` to aggregate saved episode JSON bundles into outcome, label, step, and metric summaries for failure/weakness analysis.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_collect_baseline_selfplay_team.batch` as the standard baseline self-play trajectory collection run.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_baseline_weakness_baseline_vs_baseline.batch` and `scripts/batch/experiments/soccerstwos_h100_cpu32_baseline_weakness_baseline_vs_random.batch` as standard baseline weakness-analysis runs.
- Handoff documentation and project governance files, including `CLAUDE.md`, the `docs/` hub, the agent template, and `scripts/setup/setup.sh`.
- `cs8803drl/core/checkpoint_utils.py` to centralize RLlib checkpoint parsing and restore helpers shared by training and evaluation code.
- Archived upstream example scripts under `examples/` so the root directory stays focused on active project entry points.
- `soccer_info.py` to centralize Soccer-Twos match-info parsing and dependency-light reward-shaping logic.
- `scripts/tools/build_agent_module.py` to build submission-ready agent modules from RLlib checkpoints.
- `scripts/setup/setup_h100_overlay.sh` to build an H100-compatible overlay env without mutating the legacy `soccertwos` base env.
- `cs8803drl/branches/role_specialization.py`, `cs8803drl/training/train_ray_role_specialization.py`, and `cs8803drl/deployment/trained_role_agent.py` for role-specialized PPO experiments.
- `cs8803drl/branches/shared_role_token.py`, `cs8803drl/training/train_ray_shared_policy_role_token.py`, and `cs8803drl/deployment/trained_shared_role_agent.py` for the shared-policy multi-agent PPO + role-token branch.
- `cs8803drl/branches/obs_summary.py`, `cs8803drl/training/train_ray_team_vs_random_summary_obs.py`, `cs8803drl/deployment/trained_summary_ray_agent.py`, and `scripts/batch/experiments/soccerstwos_h100_cpu32_summary_obs.batch` for the stateless ray-summary single-player PPO branch.
- `cs8803drl/branches/lstm_transfer.py`, `cs8803drl/training/train_ray_team_vs_random_lstm.py`, `cs8803drl/deployment/trained_lstm_ray_agent.py`, and `scripts/batch/experiments/soccerstwos_h100_cpu32_lstm.batch` for the recurrent single-player PPO branch.
- `cs8803drl/branches/shared_central_critic.py`, `cs8803drl/training/train_ray_shared_central_critic.py`, `cs8803drl/deployment/trained_shared_cc_agent.py`, and `scripts/batch/experiments/soccerstwos_h100_cpu32_shared_cc.batch` for the shared-actor PPO + centralized-critic branch.
- `scripts/eval/evaluate_official_suite.py` and `scripts/eval/evaluate_official_scan.py` to wrap the official `soccer_twos.evaluate` workflow for large-sample checkpoint selection.
- `docs/experiments/artifacts/` and new snapshots documenting evaluator realignment plus role/shared-policy ablations.
- `snapshot-005-observation-memory-and-centralized-critic-ablation.md` to record the summary-observation, recurrent PPO, and centralized-critic results.
- `docs/management/directory-governance.md` to define root-directory exceptions, artifact placement rules, and a safe two-phase cleanup plan.
- `scripts/README.md` to document what belongs under `scripts/` and what must remain in the repo root for runtime compatibility.
- `snapshot-006-fixed-teammate-and-dual-expert-rethink.md` to record the dual-expert / fixed-teammate mainline rethink and the ongoing 2026-04-11 follow-up runs.
- `scripts/batch/mainline/soccerstwos_h100_cpu32_singleplayer_fixed_teammate_scratch.batch` for a clean fixed-teammate initialization ablation (`256,128`, same teammate, scratch vs warm-start only).
- `snapshot-007-base-lane-reset-and-directory-reorg.md` to record the SSOT/starter reset, clean base-vs-adaptation framing, and the structured package/script reorganization.
- `snapshot-008-starter-aligned-base-model-lane.md` to record the clean starter-aligned base-model lane and its scratch-only training scripts.
- `cs8803drl/training/train_ray_base_team_vs_random.py`, `cs8803drl/training/train_ray_base_ma_teams.py`, and `scripts/batch/base/*` for the new starter-aligned base-model lane.
- `cs8803drl/deployment/trained_team_ray_agent.py` and base-team batch defaults so `team_vs_random` base checkpoints are evaluated with the correct team-level deployment wrapper.
- `cs8803drl/deployment/trained_ma_team_agent.py` and the Base-C batch/training defaults so `multiagent_team` base checkpoints are evaluated with the correct shared-policy multiagent wrapper.
- `cs8803drl/training/train_ray_base_team_vs_baseline.py` plus `scripts/batch/base/soccerstwos_h100_cpu32_base_team_vs_baseline_512x512.batch` and `scripts/batch/base/soccerstwos_h100_cpu32_base_team_vs_baseline_512x256.batch` for the baseline-targeted scratch base-model lane.
- `snapshot-009-base-team-vs-baseline-lane.md` to record the clean Base-D / Base-E comparison, the reward-vs-win-rate interpretation, and the resulting `512x512 > 512x256` conclusion.
- `cs8803drl/evaluation/failure_cases.py` and `scripts/eval/capture_failure_cases.py` to capture structured losing-episode traces, shaping debug, and heuristic failure labels for reward-design analysis.
- `snapshot-010-shaping-v2-deep-zone-ablation.md` to record shaping-v1 official-500 results, failure-case analysis, horizon reassessment, and the pre-registered v2 deep-zone / negative-C ablation plan.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_team_vs_baseline_shaping_v1_scratch_512x512.batch` and `scripts/batch/experiments/soccerstwos_h100_cpu32_team_vs_baseline_shaping_v2_deepzone_scratch_512x512.batch` for an explicit v1-vs-v2 shaping A/B on the `team_vs_baseline` scratch line.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_team_vs_baseline_shaping_v3_progress_gated_scratch_512x512.batch` plus a `progress_requires_possession` shaping toggle so the next ablation can gate positive ball-progress reward on confirmed possession.

### Changed
- `docs/experiments/README.md`, `docs/README.md`, and `docs/plan/plan-002-il-mappo-dual-mainline.md` now index `SNAPSHOT-025` as the next logical follow-up after `024`: a champion-on-champion fine-tune test rather than another mechanism-discovery lane.
- `docs/experiments/snapshot-022-role-differentiated-shaping.md` and `docs/experiments/snapshot-024-striker-defender-role-binding.md` now also preserve the old “near-scratch because warm-start silently failed” baseline/failure-capture evidence as append-only contrast sections, clarifying that both asymmetry lines were weak without a real warm-start floor and that `024` is materially more warm-start-dependent than `022`.
- `docs/experiments/snapshot-022-role-differentiated-shaping.md` 现在已收口修复 warm-start 后的正式 rerun：official `baseline 500` 最好点为 `checkpoint-280 = 0.818`，failure capture 确认这条线显著压低了 `low_possession`，但 head-to-head 结果显示其仍低于 `017@2100` 与 `024@270`。
- `docs/experiments/snapshot-024-striker-defender-role-binding.md` 现在已收口修复 warm-start 后的正式 rerun：official `baseline 500` 最好点为 `checkpoint-270 = 0.842`，failure capture 将其确认成平衡的冠军级挑战者，head-to-head 显示其已小优 `018@290`，但仍低于 `017@2100`。
- `docs/experiments/README.md` 现在把 `022/024` 从“已修复待重跑”更新为“已完成修复后首轮结果”，并明确当前 repaired 版本排序为 `024 > 022`。
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md` and `docs/architecture/code-audit-001.md` now make the official API constraint explicit: submission-time agents only receive `act(obs)` inputs, never `info`. As a result, `021c` is no longer a vague “official-aligned” lane; it is now pre-registered primarily as an auxiliary teammate-prediction route where `info` may serve only as a training label, not as inference input.
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md` now records the completed `021b` local true-info diagnostic: `checkpoint-390` achieved only `0.135 @ 200 ep` vs baseline, tightening the verdict from “eval mismatch only” to “the naive true-info teammate+time concatenation route itself did not learn a viable policy,” while leaving `021c` as the remaining official-aligned avenue.
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md`, `docs/experiments/README.md`, and `docs/README.md` now replace the interim `021a/021b` wording with the cleaner `021b/021c` split: `021b` is a local real-info diagnostic lane that keeps the original teammate+time semantics but no longer uses the decoder, while `021c` is the future official-aligned rerun where training and deployment must share the same inference-available teammate signal.
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md`, `docs/experiments/README.md`, and `docs/README.md` now split the old single-lane 021 story into `021a` (completed diagnostic run) and `021b` (official-aligned rerun design), so the anomalous first result is no longer misread as a clean rejection of the obs hypothesis.
- `docs/experiments/snapshot-022-role-differentiated-shaping.md`, `docs/experiments/snapshot-024-striker-defender-role-binding.md`, and `docs/experiments/README.md` now explicitly distinguish the invalid first-run `022/024` results from the repaired lane state: the shared-cc warm-start bug is documented, the fix is recorded, and both snapshots are now marked as “repaired, pending rerun” instead of still merely “bug found”.
- `docs/README.md`, `docs/experiments/README.md`, and `plan-002-il-mappo-dual-mainline.md` now index SNAPSHOT-020 as the gating experiment for any future `FrozenTeamCheckpointPolicy` work: first validate a MAPPO-native v4-style line, then decide whether a team-level PPO-v4 adapter is worth the engineering cost.
- `docs/README.md` and `docs/experiments/README.md` now index SNAPSHOT-019 as the next aggressive opponent-pool rebalance lane after SNAPSHOT-018, while keeping the v4-style opponent idea explicitly deferred to a later, separate adaptation experiment.
- Expanded `snapshot-018-mappo-v2-opponent-pool-finetune.md` from an active Phase-1 record into a completed first-round result record: static frozen-pool fine-tune now documents the official `baseline 500` sweep (`ckpt290 = 0.812`), failure-capture mechanism analysis, and head-to-head / failure-capture comparisons against `MAPPO+v1`, `MAPPO no-shaping`, and `MAPPO+v2`.
- `docs/experiments/README.md` now indexes SNAPSHOT-018 as a completed first-round line rather than an in-progress smoke-only robustness lane.
- Expanded `snapshot-018-mappo-v2-opponent-pool-finetune.md` from pure preregistration into an active Phase-1 record: the line now reflects the real `multiagent_player` MAPPO semantics, documents the implemented static frozen-pool design (without rolling self yet), and records a successful 1-iteration smoke with warm-start plus frozen-opponent loading.
- `docs/experiments/README.md` and `plan-002-il-mappo-dual-mainline.md` now index SNAPSHOT-018 as an active parallel robustness lane instead of leaving it as a disconnected preregistration.
- Tightened the default `scripts/batch/experiments/soccerstwos_h100_cpu32_bc_player_baseline_selfplay_512x512.batch` loader settings (`NUM_LOADER_WORKERS=0`) after the first full player-BC run hit a memcg OOM kill despite correct code paths.
- Expanded `snapshot-017-bc-to-mappo-bootstrap.md` from pure preregistration into an active bridge-validation record: player-level BC checkpoints can now be derived from existing team teacher shards, `BC_WARMSTART_CHECKPOINT` is wired into the fair MAPPO trainer, and the first `BC -> MAPPO` 1-iteration smoke completed with a successful `[bc-warmstart]` actor initialization plus checkpoint writeout.
- Expanded `docs/experiments/snapshot-016-shaping-v4-survival-anti-rush-ablation.md` from preregistration into a completed first-round result record: official `baseline 500` now shows a stable `0.766~0.768` plateau, making `v4` the strongest PPO shaping line while still trailing the current MAPPO best.
- Expanded `docs/experiments/snapshot-016-shaping-v4-survival-anti-rush-ablation.md` again with `v4`-vs-`v2` failure-capture diagnostics: the new survival / anti-rush terms clearly lengthen losing episodes and reduce `late_defensive_collapse`, but they also shift more failures into `low_possession` and `poor_conversion`, clarifying that `v4` fixes loss tempo more than attacking quality.
- Expanded `snapshot-014-mappo-fair-ablation.md` from preregistration into a completed first-round MAPPO benchmark: all three lines were trained and officially re-evaluated with `baseline 500`, and the resulting conclusion is now explicit: `MAPPO + shaping-v2` currently matches the best project-level official score while `MAPPO + no shaping` still serves as a meaningful mechanism control.
- Expanded `snapshot-011-shaping-v3-progress-gated-ablation.md` from pure preregistration into a completed first-round record: the v3 run finished, internal `baseline 50` topped out at `0.70`, and the line is now explicitly closed as a negative result with `v2` retained as the better base for any future `v4`.
- Updated `README.md`, `docs/README.md`, `docs/experiments/README.md`, `scripts/README.md`, `snapshot-012-imitation-learning-bc-bootstrap.md`, and `plan-002-il-mappo-dual-mainline.md` so the teacher-data phase is marked complete and the repository now indexes the concrete BC trainer / wrapper / batch from `SNAPSHOT-015`.
- Redirected the three new MAPPO L1a Slurm batch logs into `docs/experiments/artifacts/slurm-logs/` instead of leaving `.out` files in the repo root.
- Expanded `snapshot-014-mappo-fair-ablation.md` with mechanism-level MAPPO success criteria derived from `SNAPSHOT-013`: not only official `baseline 500`, but also whether failure buckets become more baseline-like, especially around `low_possession`, `poor_conversion`, and losing-episode length.
- Expanded `snapshot-014-mappo-fair-ablation.md` from pure preregistration into a preregistration plus concrete training-entry/batch definition for L1a.
- Updated `docs/plan/README.md`, `README.md`, and `docs/README.md` so `PLAN-002` replaces `PLAN-001` as the current top-level route while preserving `PLAN-001` as historical context.
- Revised `snapshot-005-observation-memory-and-centralized-critic-ablation.md` so the centralized-critic result is no longer treated as a true negative; summary-observation and LSTM remain negative, but old `shared_cc_warm225` is now explicitly marked as an orthogonal, non-fair test that does not reject MAPPO.
- Expanded `plan-001-il-baseline-exploitation.md` with a dedicated MAPPO / centralized-critic validation lane instead of lumping all algorithm changes into a deferred bucket.
- Updated `docs/README.md`, `docs/experiments/README.md`, `scripts/README.md`, and the root `README.md` so the new `docs/plan/` area plus the IL/weakness-analysis workstreams are indexed alongside existing snapshots and tooling.
- Expanded `snapshot-013-baseline-weakness-analysis.md` with the first concrete `baseline vs baseline` / `baseline vs random` findings: baseline is strongest in short-to-mid games, but its main exploitable weakness is long-game drift and late defensive collapse rather than early collapse.
- Rewrote the root `README.md` and `docs/README.md` to reflect the current CS8803 project structure and workflows.
- Added H100 overlay environment guidance and GPU validation references to the setup/deployment docs.
- Updated training and evaluation scripts to reuse shared checkpoint-loading utilities instead of duplicating restore logic.
- Refreshed the PACE batch script and `.gitignore` to better support reproducible experiments and keep generated artifacts out of git.
- Clarified assignment SSOT, PACE cluster rules, deployment steps, and project priorities in `CLAUDE.md`, `docs/management/ROADMAP.md`, and `docs/management/deploy-and-verify.md`.
- Hardened the agent template and legacy trained-agent wrapper with more robust `ActionFlattener` imports and per-agent dummy env registration.
- Archived official-evaluator logs, scan CSVs, and SLURM outputs under `docs/experiments/artifacts/` so the repo root stays focused on entry points and source modules.
- Refreshed the root and docs indexes to reflect the current experimental branches, agent wrappers, and directory-governance rules.
- `scripts/soccerstwos_h100_cpu32_singleplayer_fixed_teammate.batch` now defaults to visible console output so early startup failures do not get silently swallowed.
- Documented that the `512,256` fixed-teammate run is a confounded width+initialization experiment and should not be used as a clean architecture conclusion.
- Physically reorganized runtime code under `cs8803drl/{core,training,deployment,evaluation,branches}` and split scripts into `scripts/{setup,eval,tools,batch/...}` with updated module paths.
- Main PPO training scripts now emit a post-run `training_loss_curve.svg` diagnostic plot from `progress.csv` plus checkpoint-eval data.
- Evaluation-side failure analysis can now persist per-episode JSON bundles with sampled/tail traces, score/winner summaries, shaping debug fields, and heuristic labels such as `late_defensive_collapse` and `poor_conversion`.
- `snapshot-010` now records the completed v1-vs-v2 deep-zone A/B, top-5%-plus-ties `baseline 500` re-evaluation, and the rerun failure-case analysis with corrected v2 shaping diagnostics.
- `docs/architecture/engineering-standards.md` now codifies the checkpoint-selection protocol: training-internal `baseline 50` only for candidate filtering, then `top 5% + ties`, then official `baseline 500`, with `random 500` used only as a final confirmation for shortlisted finalists.
- `snapshot-011-shaping-v3-progress-gated-ablation.md` to preregister the v3 ablation: keep v2 defensive shaping fixed and only gate positive ball-progress reward on confirmed possession.
- Reward shaping config parsing now accepts explicit deep-zone penalty parameters, and the baseline-shaping trainer prints the active shaping coefficients in its run header for easier A/B verification.
- Reward shaping now also supports two new v4 controls: a possession-confirmed defensive-survival bonus in pressured defending states and a terminal fast-loss penalty keyed to short losing episodes; the new shaping-v4 batch explicitly locks `progress_requires_possession=0` so the run definition does not depend on inherited defaults.
- Added a `ray_summary_obs` environment wrapper path so single-player PPO can append reproducible ray-summary observation features without relying on `info` or episode state.
- Added a partial warm-start path from the feed-forward single-player checkpoint into an LSTM PPO policy by reusing matching MLP and action/value head weights.
- Added a centralized-critic warm-start path that reuses the single-player actor trunk and adapts the critic input layer for own-observation, teammate-observation, and teammate-action features.

### Fixed
- `cs8803drl/branches/shared_central_critic.py` now handles shared-cc → shared-cc warm-start correctly by directly copying matching `action_model.* / value_model.*` tensors instead of incorrectly assuming a bare-trunk source checkpoint; post-fix smoke now reports `copied=16, adapted=0, skipped=0`.
- `train_bc_team_policy.py` no longer uses Python-3.10-only `Path | None` annotations, and `MAX_SAMPLES` now short-circuits shard loading instead of concatenating the full 573k-sample dataset before subsampling, which makes BC smoke validation practical on the current stack.
- `scripts/tools/collect_baseline_trajectories.py` now emits periodic collector progress and writes `manifest.partial.json` during long baseline self-play jobs, so IL data collection no longer appears stalled until final cleanup writes `manifest.json`.
- Fixed reward shaping so ball-progress and opponent-progress terms use a stable previous-ball snapshot instead of silently zeroing one branch.
- Fixed reward shaping in `single_player=True` training mode so per-agent shaping can be aggregated back into the scalar team reward.
- Reduced silent drift between training/evaluation scripts by sharing a single score/winner parsing implementation.
- Confirmed the project can again complete a 1-iteration GPU smoke train on H100 via the overlay env after the legacy `torch 1.8.1+cu102` stack failed with `no kernel image`.
- Fixed evaluation drift by aligning `evaluate_matches.py` with official side-swapping and team-local agent-id semantics.
- Guarded official evaluator wrapper scripts against invalid single-episode runs and port-overflow scan configurations.
- Validated the summary-observation branch with a 1-iteration warm-start smoke run plus an official-evaluator smoke check through `trained_summary_ray_agent`.
- Validated the LSTM branch with a 1-iteration warm-start smoke run plus an official-evaluator smoke check through `trained_lstm_ray_agent`.
- Validated the centralized-critic branch with a 1-iteration warm-start smoke run plus an official-evaluator smoke check through `trained_shared_cc_agent`.
- Fixed training-time checkpoint evaluation port allocation so long runs no longer overflow past `65535`; monitor-driven evals and `backfill_run_eval.py` now wrap within a safe port band instead of monotonically adding `20` forever.
- Fixed `train_ray_base_team_vs_baseline.py` post-run summary handling so completed baseline-lane runs no longer crash during the final `_print_progress(...)` call after checkpoints and metrics have already been written.
- Fixed failure-case debug extraction so possession/progress metadata is still recorded even when `opponent_progress_penalty_scale=0`, preventing blind spots in shaping-v1 diagnostics.
- Added symmetric deep-zone danger penalties to the shaping helper without changing existing behavior when the new coefficients remain zero, preserving old runs while enabling the v2 ablation.
- `evaluate_matches.py` now prints step-length statistics for all episodes, wins, and losses, and writes a `summary.json` alongside saved failure-case episodes for easier post-hoc comparison.
- Failure-capture tooling now supports `--progress-requires-possession`, keeping evaluation/debug shaping aligned with the new v3 possession-gated progress setting.
- `scripts/eval/capture_failure_cases.py` no longer requires a checkpoint when capturing baseline-only matchups, which makes baseline-vs-baseline and baseline-vs-random weakness studies a first-class workflow.

### Removed
- Deleted tracked `ray_results/` artifacts and the root-level `ceia_baseline_agent.zip` archive from version control.
- Removed root-level legacy example scripts after archiving them under `examples/`.
- Removed repository `__pycache__/` directories as part of the worktree cleanup pass.
- `docs/experiments/snapshot-020-mappo-v4-fair-ablation.md` now records the merged first-round reading of MAPPO+v4: the original run produced the strongest internal `baseline 50` window (`0.86 @ 360/430`), while the resume run finished cleanly to `500 iter` but did not surpass those mid/late scratch peaks, so the formal `baseline 500` recheck should still target the original run's `360/420/430` checkpoints.
- `docs/experiments/snapshot-019-mappo-v2-opponent-pool-anchor-heavy-rebalance.md` now records the merged first-round reading of the anchor-heavy pool lane: the original run produced the strongest early window (`0.88 @ 40`, `0.86 @ 60`), while the resume run finished cleanly to `300 iter` but did not sustain or exceed those early peaks, so formal `baseline 500` recheck should still center on `40/60/140` with `230` only as a resume-side reference point.
- `docs/experiments/snapshot-017-bc-to-mappo-bootstrap.md` now records the full first-round `BC -> MAPPO + shaping-v2` outcome: the formal run completed to `2335 iter`, the expanded `baseline 500` recheck surfaced a new project-best `checkpoint-2100 = 0.842`, and the four-point failure-capture comparison (`2100/2250/2170/1870`) identified `2100` as the most balanced and trustworthy champion with `2170` as the strongest backup.
- `docs/experiments/snapshot-021-actor-teammate-obs-expansion.md` now upgrades the planned minimal obs variant from `+4 teammate` to `+5 teammate+time`, so the root-cause-A test no longer leaves episode-time blindness as an unaddressed confound.
- `docs/experiments/snapshot-022-role-differentiated-shaping.md` now records a 20-reset spawn-stability smoke: training-side `agent_id 0/1` start positions were perfectly stable, so the current role binding does not require immediate code changes before first launch.
- `docs/experiments/snapshot-013-baseline-weakness-analysis.md` §12.2 now states the stronger post-audit reading more precisely: BC results further strengthen the obs-bottleneck story, but reward-symmetry evidence is also strengthened rather than ruled out.
- `docs/experiments/snapshot-022-role-differentiated-shaping.md` is now explicitly framed as an **agent-id asymmetry** experiment rather than a strict striker/defender role claim, and new [snapshot-024-striker-defender-role-binding.md](docs/experiments/snapshot-024-striker-defender-role-binding.md) captures the later, stricter field-role version.
- Implemented the first runnable `024` plumbing for field-role-bound MAPPO shaping: [cs8803drl/branches/role_specialization.py](cs8803drl/branches/role_specialization.py) now builds field-role shaping configs, [cs8803drl/core/utils.py](cs8803drl/core/utils.py) can resolve per-episode `role_by_agent` from spawn-depth semantics, and [soccerstwos_h100_cpu32_mappo_field_role_binding_v2_warm470_512x512.batch](scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_field_role_binding_v2_warm470_512x512.batch) is ready for first run.
- `snapshot-021` and `code-audit-001` now record a tighter post-smoke reading of the teammate-obs failure: env `info` extraction is confirmed correct, decoder train-eval drift is still real but downgraded to a secondary risk, and the current strongest root-cause hypothesis is unnormalized teammate-tail dominance (`raw own_obs ∈ [0,1]` vs teammate tail in field units up to roughly `±15/±8`).
- Added optional teammate-tail normalization plumbing for obs-expansion lanes: [obs_teammate.py](cs8803drl/core/obs_teammate.py) now accepts a configurable `state_scale`, [TeammateStateObsWrapper](cs8803drl/core/utils.py) passes it through training envs, [evaluate_teammate_obs_local.py](scripts/eval/evaluate_teammate_obs_local.py) supports the same scale for `021b` local diagnostics, [trained_shared_cc_teammate_obs_agent.py](cs8803drl/deployment/trained_shared_cc_teammate_obs_agent.py) applies it to decoder fitting for internal eval alignment, and a dedicated normalized rerun batch is available at [soccerstwos_h100_cpu32_mappo_teammate_obs_norm_scratch_512x512.batch](scripts/batch/experiments/soccerstwos_h100_cpu32_mappo_teammate_obs_norm_scratch_512x512.batch).
- `docs/experiments/snapshot-025-bc-champion-field-role-binding.md` now records the completed first-run official and failure-capture verdict for `025`: official `baseline 500` peaks at `checkpoint-140 = 0.822`, but `checkpoint-20 = 0.818` is now treated as the more trustworthy main candidate because it aligns much better with `500`-episode failure capture (`0.806`) and keeps `poor_conversion` / `unclear_loss` lower than the later-window points.
- `docs/experiments/README.md` now indexes `SNAPSHOT-025` as a completed first-run result rather than a preregistration, explicitly framing it as a “short-horizon beneficial fine-tune on top of the champion base” instead of a new champion lane.
- `scripts/batch/experiments/soccerstwos_h100_cpu32_team_level_v2_scratch_768x512.batch` to operationalize `SNAPSHOT-027-A` without inventing a new training stack: it reuses the existing `team_vs_policy` shaping-v2 trainer and team-level deployment wrapper, sets the team MLP to `768,512`, keeps the native `MultiDiscrete([3,3,3,3,3,3])` action space, and budgets the first scratch coordination run at `50M` steps / `16h`.
- `docs/experiments/snapshot-027-team-level-ppo-coordination.md` now records completed preflight validation for `027`: real env smoke confirmed `obs_shape=(672,)`, `action_space=MultiDiscrete([3,3,3,3,3,3])`, and visible shaping debug in `team_vs_policy`, so the lane is no longer “待实现” but “027-A 可运行”.
- `docs/experiments/README.md` now indexes `SNAPSHOT-027` as runnable via the new `027-A` batch rather than purely preregistered.
- `docs/experiments/snapshot-028-team-level-bc-to-ppo-bootstrap.md` now states the stronger and more accurate scope for `028`: it is a **team-policy-to-team-policy** IL→BC→PPO SOTA comparison, so player-level BC assets from `017` are not directly reusable, existing team-level dataset / BC assets are only scaffolding or smoke inputs, and the real missing engineering work is the team-level `BC_WARMSTART_CHECKPOINT` path into PPO. The first formal lane is correspondingly narrowed to `028-A = 512x512`, with `768x512` reframed as a later capacity extension rather than the prerequisite mainline.
- Implemented the missing `028` Stage-3 bridge: [imitation_bc.py](cs8803drl/branches/imitation_bc.py) now provides `warmstart_team_level_policy_from_bc()`, [train_ray_team_vs_baseline_shaping.py](cs8803drl/training/train_ray_team_vs_baseline_shaping.py) accepts `BC_WARMSTART_CHECKPOINT` and writes `warmstart_summary.txt`, and [train_bc_team_policy.py](cs8803drl/training/train_bc_team_policy.py) now supports multi-dataset team BC training via `DATASET_DIRS`. Added four `028-A` batch scripts covering teacher self-play collection, teacher-vs-random collection, team BC, and team PPO warm-start.
- Completed focused `028-A` warm-start validation with the existing team-level BC scaffold: a 1-iteration smoke run at [PPO_team_level_bc_bootstrap_028A_smoke2_20260417_104026](ray_results/PPO_team_level_bc_bootstrap_028A_smoke2_20260417_104026) confirmed `status: bc_warmstart_applied`, `copied=6, adapted=0, skipped=0` in [warmstart_summary.txt](ray_results/PPO_team_level_bc_bootstrap_028A_smoke2_20260417_104026/warmstart_summary.txt), and successfully wrote a PPO checkpoint, advancing `028` from pure preregistration to an implementation-ready lane.
- `docs/experiments/snapshot-026-reward-liberation-ablation.md` now records the tightened post-follow-up reading of `026B`: `checkpoint-250` remains the official peak (`0.864`), but additional `500ep` failure capture on `checkpoint-170/280` shows `checkpoint-170 = 0.842 official / 0.822 capture` is the more trustworthy main candidate, with much better official-capture alignment and substantially lower `low_possession`. `026` is therefore now framed as a PBRS lane with real failure-structure gains, but still below the current champion line.
- Added robust merged-summary support for quota-resumed runs. [training_plots.py](cs8803drl/core/training_plots.py) now merges multiple `progress.csv` segments, strips NUL corruption, and filters backward-corrupted rows; [print_merged_training_summary.py](scripts/tools/print_merged_training_summary.py) now emits canonical `merged_training_summary.txt` plus `training_loss_curve_merged.png` for resumed experiment roots; and the 027/028/029 snapshots now explicitly document where each run broke, which checkpoint the continuation used, and why future readers should interpret merged summaries instead of per-trial terminal output. `027A` additionally records the special case that `checkpoint-700` was corrupted and the successful continuation used `checkpoint-690`.
- Clarified the documentation contract for resumed experiments: `docs/experiments/README.md` and snapshots `027/028/029` now state explicitly that the canonical post-resume artifact pair is **`merged_training_summary.txt` + `training_loss_curve_merged.png`**, so readers know to use the merged curve rather than any single-trial legacy loss plot.
